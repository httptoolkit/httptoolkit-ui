import * as React from "react";
import styled from 'styled-components';

export const RequestDetailsPane = styled(() => <div>
    Select some requests to see their details here.
</div>)`
    background-color: #444;
`;